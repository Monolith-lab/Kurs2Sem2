Для поиска корней необходимо запустить скрипт MATLAB, введя свои значения чисел Фурье и Био, которые были рассчитаны в файле SMath, а также указать путь,
куда сохранять файл эксель, путь задаётся переменной filename_excel.
Для того, чтобы импортировать данные из файла эксель в SMath, необходимо указать полный путь нахождения файла эксель(включая название),
файл по умолчанию будет располагаться в той же папке, где расположен скрипт MATLAB.
